{
  "name": "anchor",
  "version": "1.0.0",
  "private": true,
  "description": "Anchor — Tokyo trip concierge. iOS wrapper (Capacitor) around the web app in ./public.",
  "scripts": {
    "ios:add": "cap add ios",
    "ios:sync": "cap sync ios",
    "ios:open": "cap open ios",
    "ios:assets": "npx @capacitor/assets@latest generate --ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
