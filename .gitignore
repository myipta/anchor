.DS_Store
public/vendor/node_modules/
public/vendor/package.json
public/vendor/package-lock.json

# Cloudflare / secrets — never commit keys
.dev.vars
.wrangler/
node_modules/

# Capacitor / iOS (generated locally on a Mac via `npx cap add ios`)
ios/
dist/
